# SPDX-FileCopyrightText: 2025 NONE
#
# SPDX-License-Identifier: Unlicense

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: docker-registry-tls
  namespace: docker-registry
spec:
  secretName: docker-registry-tls
  issuerRef:
    name: ca
    kind: ClusterIssuer
  dnsNames:
  - registry.ajphome.com
